@model ABC_Pharmacy.Models.Medicine

@{
    ViewData["Title"] = "Buy";
}

<h1>Buy</h1>

@using (Html.BeginForm("Buy", "Medicines", FormMethod.Post))
{
    ABC_Pharmacy.Models.Medicine medicine = new ABC_Pharmacy.Models.Medicine();
    <div>
        <h4>Medicine</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Name)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Quantity)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Quantity)
            </dd>

        </dl>
    </div>
    <div>
        @Html.ActionLink("Buy", "Buy") |
        <a asp-action="GetMedicines">Back to List</a>
    </div>
}
